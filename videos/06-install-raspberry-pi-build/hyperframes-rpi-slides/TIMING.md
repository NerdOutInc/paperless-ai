# Raspberry Pi Install HyperFrames Timing

This reel is rendered once and split into two clips for the revision.

| Time | Use | Notes |
| --- | --- | --- |
| `00:00-00:03` | Intro title card | Existing title image, stable before fade. |
| `00:03-00:20` | Intro slide 1 | Paperless-ngx stays stock; Paperless AI is the companion layer. Fade in from the title card, then out cleanly before slide 2. |
| `00:20-00:31` | Intro slide 2 | Why a Raspberry Pi works for a modest local archive. Fade to screencast happens in the edit render, starting after `00:30`. |
| `00:31-00:41` | Insert slide | Move the flashed card or SSD into the Pi, connect network, power on. |
| `00:41-00:58` | Recap slide | Checklist for the closing recap before the existing outro card. |

The final screencast preview uses `00:00-00:31` at the beginning with the
screencast fade starting after `00:30`, and `00:31-00:41` around output time
`02:33-02:42`. The recap slide is inserted from `06:30.52-06:47.48`, and the
existing outro card lands on the final thank-you line.
