# DigitalOcean 1-Click Droplet Image Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> superpowers:subagent-driven-development (recommended) or
> superpowers:executing-plans to implement this plan task-by-task. Steps
> use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a pre-baked DigitalOcean snapshot with a web setup wizard
so non-technical users can deploy Paperless AI by clicking a single link.

**Architecture:** Packer builds an Ubuntu 24.04 snapshot with Docker and
all images pre-pulled. On first boot, a host-level Caddy + Python API
serves a setup wizard on port 80. After the user submits the form, the
API generates configs, starts Docker Compose, stops the wizard, and hands
off to the production Caddy container.

**Tech Stack:** Packer HCL, Bash, Python 3 stdlib, Caddy, Docker Compose,
GitHub Actions

**Spec:** `docs/superpowers/specs/2026-04-06-digitalocean-one-click-design.md`

---

## File Map

### New files to create

```text
packer/
    paperless-ai.pkr.hcl             Packer template (DO builder config)
    provision.sh                     Provisioning script run during image build
    docker-compose.pull.yml          Minimal compose file for pre-pulling images

one-click/
    setup/
        wizard.html                  Static HTML setup form
        setup-api.py                 Python 3 stdlib HTTP handler
        Caddyfile.setup              Caddy config for wizard mode
    templates/
        docker-compose.yml.tpl       Docker Compose template with placeholders
        env.template                 .env file template
        Caddyfile.domain.tpl         Production Caddyfile with domain (HTTPS)
        Caddyfile.ip.tpl             Production Caddyfile without domain (HTTP)
    scripts/
        first-boot.sh                Cloud-init entrypoint
        finalize-setup.sh            Called by setup-api.py to run docker compose
        update.sh                    Pull latest images + restart
        backup.sh                    pg_dump wrapper
        restore.sh                   pg_restore wrapper
    systemd/
        paperless-setup.service      Systemd unit for host Caddy (wizard)
        paperless-setup-api.service  Systemd unit for Python setup API

.github/workflows/
    packer-build.yml                 CI workflow for snapshot builds

docs/
    deploy-config.json               Snapshot ID for deploy link (auto-updated by CI)

```

### Existing files to modify

```text
docs/index.html                      Add "Deploy on DigitalOcean" button

```

---

## Task 1: Config Templates

Create the `.env`, `docker-compose.yml`, and `Caddyfile` templates that
the setup wizard will render with user-provided values.

**Files:**

- Create: `one-click/templates/env.template`
- Create: `one-click/templates/docker-compose.yml.tpl`
- Create: `one-click/templates/Caddyfile.domain.tpl`
- Create: `one-click/templates/Caddyfile.ip.tpl`

- [ ] **Step 1: Create env.template**

```text
# Paperless AI configuration -- generated by setup wizard
# Do not share this file. It contains passwords.

ADMIN_USER='{{ADMIN_USER}}'
ADMIN_PASSWORD='{{ADMIN_PASSWORD}}'
DB_PASSWORD='{{DB_PASSWORD}}'
SECRET_KEY='{{SECRET_KEY}}'
TIMEZONE='{{TIMEZONE}}'
PAPERLESS_URL='{{PAPERLESS_URL}}'
MCP_AUTH_TOKEN='{{MCP_AUTH_TOKEN}}'

```

Write to `one-click/templates/env.template`. Uses `{{VAR}}` placeholders
(not `${VAR}`) to avoid shell expansion issues. The setup API does simple
string replacement.

- [ ] **Step 2: Create docker-compose.yml.tpl**

```yaml
services:
  db:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: paperless
      POSTGRES_USER: paperless
      POSTGRES_PASSWORD: {{DB_PASSWORD}}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U paperless"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  paperless:
    image: ghcr.io/paperless-ngx/paperless-ngx:latest
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - paperless-data:/usr/src/paperless/data
      - paperless-media:/usr/src/paperless/media
      - paperless-export:/usr/src/paperless/export
      - paperless-consume:/usr/src/paperless/consume
    environment:
      PAPERLESS_REDIS: redis://redis:6379
      PAPERLESS_DBHOST: db
      PAPERLESS_DBNAME: paperless
      PAPERLESS_DBUSER: paperless
      PAPERLESS_DBPASS: {{DB_PASSWORD}}
      PAPERLESS_SECRET_KEY: {{SECRET_KEY}}
      PAPERLESS_TIME_ZONE: {{TIMEZONE}}
      PAPERLESS_URL: {{PAPERLESS_URL}}
      PAPERLESS_ADMIN_USER: {{ADMIN_USER}}
      PAPERLESS_ADMIN_PASSWORD: {{ADMIN_PASSWORD}}
    healthcheck:
      test: ["CMD", "curl", "-fs", "http://localhost:8000"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 90s

  companion:
    image: ghcr.io/nerdoutinc/paperless-ai:latest
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      paperless:
        condition: service_healthy
    environment:
      PAPERLESS_API_URL: http://paperless:8000
      PAPERLESS_USERNAME: {{ADMIN_USER}}
      PAPERLESS_PASSWORD: {{ADMIN_PASSWORD}}
      DATABASE_URL: postgresql://paperless:{{DB_PASSWORD}}@db:5432/paperless
      EMBEDDING_MODEL: all-MiniLM-L6-v2
      SYNC_INTERVAL_SECONDS: "60"
      MCP_HTTP_PORT: "3001"
      MCP_AUTH_TOKEN: {{MCP_AUTH_TOKEN}}
      PYTHONUNBUFFERED: "1"

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - paperless
      - companion

volumes:
  pgdata:
  redisdata:
  paperless-data:
  paperless-media:
  paperless-export:
  paperless-consume:
  caddy-data:
  caddy-config:

```

Write to `one-click/templates/docker-compose.yml.tpl`.

- [ ] **Step 3: Create Caddyfile templates**

`one-click/templates/Caddyfile.domain.tpl`:

```caddyfile
{{DOMAIN}} {
    tls {{LE_EMAIL}}

    @discovery path /.well-known/oauth-authorization-server /.well-known/openid-configuration
    handle @discovery {
        respond 404
    }

    @mcp path /mcp /mcp/*
    handle @mcp {
        reverse_proxy companion:3001 {
            header_up Host localhost:3001
        }
    }

    handle {
        reverse_proxy paperless:8000
    }
}

```

`one-click/templates/Caddyfile.ip.tpl`:

```caddyfile
:80 {
    @discovery path /.well-known/oauth-authorization-server /.well-known/openid-configuration
    handle @discovery {
        respond 404
    }

    @mcp path /mcp /mcp/*
    handle @mcp {
        reverse_proxy companion:3001 {
            header_up Host localhost:3001
        }
    }

    handle {
        reverse_proxy paperless:8000
    }
}

```

- [ ] **Step 4: Commit**

```bash
git add one-click/templates/
git commit -m "feat(one-click): add config templates for env, compose, and caddyfile"

```

---

## Task 2: Helper Scripts

Create the operational scripts that ship with the image. These are
adapted from the existing `docs/install.sh` generated scripts.

**Files:**

- Create: `one-click/scripts/update.sh`
- Create: `one-click/scripts/backup.sh`
- Create: `one-click/scripts/restore.sh`

- [ ] **Step 1: Create update.sh**

```bash
#!/usr/bin/env bash
set -euo pipefail
cd /opt/paperless-ai

mkdir -p backups

if docker compose ps --status running db 2>/dev/null | grep -q db; then
    echo "Backing up database before update..."
    docker compose exec -T db pg_dump --clean -U paperless paperless \
        > "backups/pre-update-$(date +%Y%m%d-%H%M%S).sql"
    echo "[OK] Database backed up"
else
    echo "[!] Database not running -- skipping backup"
fi

echo "Pulling latest images..."
docker compose pull

echo "Restarting services..."
docker compose up -d

echo ""
echo "[OK] Update complete. Check your Paperless UI to confirm."

```

Write to `one-click/scripts/update.sh`.

- [ ] **Step 2: Create backup.sh**

```bash
#!/usr/bin/env bash
set -euo pipefail
cd /opt/paperless-ai

BACKUP_DIR="backups"
TIMESTAMP=$(date +%Y%m%d-%H%M%S)
BACKUP_FILE="$BACKUP_DIR/paperless-$TIMESTAMP.sql"

mkdir -p "$BACKUP_DIR"
echo "Backing up database..."
docker compose exec -T db pg_dump --clean -U paperless paperless > "$BACKUP_FILE"
echo "[OK] Backup saved to $BACKUP_FILE"

# Keep only the last 7 daily backups
ls -t "$BACKUP_DIR"/paperless-*.sql 2>/dev/null | tail -n +8 | xargs -r rm
echo "[OK] Old backups cleaned up (keeping last 7)"

```

Write to `one-click/scripts/backup.sh`.

- [ ] **Step 3: Create restore.sh**

```bash
#!/usr/bin/env bash
set -euo pipefail
cd /opt/paperless-ai

if [[ $# -lt 1 ]]; then
    echo "Usage: bash restore.sh <backup-file.sql>"
    echo ""
    echo "Available backups:"
    ls -lh backups/*.sql 2>/dev/null || echo "  (none found)"
    exit 1
fi

BACKUP_FILE="$1"
if [[ ! -f "$BACKUP_FILE" ]]; then
    echo "Error: File not found: $BACKUP_FILE"
    exit 1
fi

echo "WARNING: This will replace your current database with the backup."
read -rp "Are you sure? [y/N]: " confirm
if [[ "${confirm,,}" != "y" ]]; then
    echo "Cancelled."
    exit 0
fi

echo "Stopping Paperless and companion..."
docker compose stop paperless companion

echo "Restoring database from $BACKUP_FILE..."
docker compose exec -T db psql -U paperless -d paperless < "$BACKUP_FILE"

echo "Restarting services..."
docker compose up -d

echo ""
echo "[OK] Database restored. Paperless is restarting."

```

Write to `one-click/scripts/restore.sh`.

- [ ] **Step 4: Commit**

```bash
chmod +x one-click/scripts/*.sh
git add one-click/scripts/
git commit -m "feat(one-click): add update, backup, and restore helper scripts"

```

---

## Task 3: Setup Wizard Backend

Create the Python API that handles form submission, generates configs,
and starts Docker Compose.

**Files:**

- Create: `one-click/scripts/finalize-setup.sh`
- Create: `one-click/setup/setup-api.py`

- [ ] **Step 1: Create finalize-setup.sh**

This script is called by `setup-api.py` after it writes config files.
It starts Docker Compose, waits for health, stops the wizard, and sets up
the backup cron.

```bash
#!/usr/bin/env bash
set -euo pipefail
cd /opt/paperless-ai

echo "Starting Paperless AI stack..."

# Stop the host Caddy so port 80 is free for the Docker Caddy
systemctl stop paperless-setup.service

docker compose up -d

echo "Waiting for Paperless to become healthy..."
timeout=300
elapsed=0
while [[ $elapsed -lt $timeout ]]; do
    if docker compose ps paperless 2>/dev/null | grep -q "(healthy)"; then
        echo "[OK] Paperless is healthy"
        break
    fi
    sleep 5
    elapsed=$((elapsed + 5))
done

if [[ $elapsed -ge $timeout ]]; then
    echo "[!] Paperless did not become healthy within ${timeout}s"
    exit 1
fi

# Disable the setup service so it never runs again
systemctl disable paperless-setup.service

# Register daily backup cron at 7 AM
(crontab -l 2>/dev/null || true; echo "0 7 * * * /opt/paperless-ai/scripts/backup.sh >> /opt/paperless-ai/backups/cron.log 2>&1") | crontab -

echo "[OK] Setup complete"

```

Write to `one-click/scripts/finalize-setup.sh` and `chmod +x`.

- [ ] **Step 2: Create setup-api.py**

```python
#!/usr/bin/env python3
"""Setup wizard API for Paperless AI 1-click image.

Handles form submission, generates config files, and kicks off
Docker Compose via finalize-setup.sh. Runs on localhost:8080,
proxied by host Caddy.

No pip dependencies -- stdlib only.
"""

import json
import os
import secrets
import string
import subprocess
import threading
from http.server import HTTPServer, BaseHTTPRequestHandler
from pathlib import Path

BASE_DIR = Path("/opt/paperless-ai")
TEMPLATES_DIR = BASE_DIR / "templates"
SETUP_STATE_FILE = BASE_DIR / ".setup-state"

# Valid IANA timezones are in /usr/share/zoneinfo -- we check against it
ZONEINFO = Path("/usr/share/zoneinfo")


def generate_secret(length=32):
    """Generate a random alphanumeric string (matches install.sh pattern)."""
    alphabet = string.ascii_letters + string.digits
    return "".join(secrets.choice(alphabet) for _ in range(length))


def valid_timezone(tz):
    """Check timezone exists in system zoneinfo."""
    if not tz or ".." in tz or tz.startswith("/"):
        return False
    return (ZONEINFO / tz).is_file()


def render_template(template_path, variables):
    """Replace {{KEY}} placeholders in a template file."""
    content = template_path.read_text()
    for key, value in variables.items():
        content = content.replace("{{" + key + "}}", value)
    return content


def get_state():
    """Read current setup state."""
    if SETUP_STATE_FILE.exists():
        return SETUP_STATE_FILE.read_text().strip()
    return "pending"


def set_state(state):
    """Write setup state."""
    SETUP_STATE_FILE.write_text(state)


class SetupHandler(BaseHTTPRequestHandler):
    def log_message(self, format, *args):
        """Suppress default access logs."""
        pass

    def _send_json(self, status, data):
        self.send_response(status)
        self.send_header("Content-Type", "application/json")
        self.end_headers()
        self.wfile.write(json.dumps(data).encode())

    def do_GET(self):
        if self.path == "/api/status":
            self._send_json(200, {"state": get_state()})
        else:
            self._send_json(404, {"error": "not found"})

    def do_POST(self):
        if self.path != "/api/setup":
            self._send_json(404, {"error": "not found"})
            return

        if get_state() != "pending":
            self._send_json(409, {"error": "setup already started"})
            return

        # Read request body
        length = int(self.headers.get("Content-Length", 0))
        body = self.rfile.read(length)
        try:
            data = json.loads(body)
        except json.JSONDecodeError:
            self._send_json(400, {"error": "invalid JSON"})
            return

        # Validate required fields
        admin_user = data.get("admin_user", "").strip()
        admin_password = data.get("admin_password", "").strip()
        timezone = data.get("timezone", "").strip()
        domain = data.get("domain", "").strip()
        le_email = data.get("le_email", "").strip()

        errors = []
        if not admin_user:
            errors.append("admin_user is required")
        if not admin_password:
            errors.append("admin_password is required")
        if len(admin_password) < 8:
            errors.append("admin_password must be at least 8 characters")
        if not timezone or not valid_timezone(timezone):
            errors.append("valid timezone is required")
        if domain and not le_email:
            errors.append("le_email is required when domain is provided")

        if errors:
            self._send_json(400, {"errors": errors})
            return

        set_state("in_progress")

        # Generate secrets
        db_password = generate_secret(32)
        secret_key = generate_secret(50)
        mcp_token = generate_secret(32)

        # Determine Paperless URL
        if domain:
            paperless_url = f"https://{domain}"
        else:
            # Get the droplet's public IP
            try:
                ip = subprocess.check_output(
                    ["curl", "-s", "-4", "http://169.254.169.254/metadata/v1/interfaces/public/0/ipv4/address"],
                    timeout=5,
                ).decode().strip()
            except Exception:
                ip = "localhost"
            paperless_url = f"http://{ip}"

        # Template variables
        variables = {
            "ADMIN_USER": admin_user,
            "ADMIN_PASSWORD": admin_password,
            "DB_PASSWORD": db_password,
            "SECRET_KEY": secret_key,
            "TIMEZONE": timezone,
            "PAPERLESS_URL": paperless_url,
            "MCP_AUTH_TOKEN": mcp_token,
            "DOMAIN": domain,
            "LE_EMAIL": le_email,
        }

        # Render config files
        env_content = render_template(
            TEMPLATES_DIR / "env.template", variables
        )
        compose_content = render_template(
            TEMPLATES_DIR / "docker-compose.yml.tpl", variables
        )

        if domain:
            caddy_content = render_template(
                TEMPLATES_DIR / "Caddyfile.domain.tpl", variables
            )
        else:
            caddy_content = render_template(
                TEMPLATES_DIR / "Caddyfile.ip.tpl", variables
            )

        # Write config files
        env_path = BASE_DIR / ".env"
        env_path.write_text(env_content)
        env_path.chmod(0o600)

        (BASE_DIR / "docker-compose.yml").write_text(compose_content)
        (BASE_DIR / "Caddyfile").write_text(caddy_content)

        # Run finalize in background so we can return the response
        # before the host Caddy shuts down
        def finalize():
            subprocess.run(
                ["/opt/paperless-ai/scripts/finalize-setup.sh"],
                capture_output=True,
            )
            set_state("complete")

        threading.Thread(target=finalize, daemon=True).start()

        self._send_json(200, {
            "status": "started",
            "mcp_token": mcp_token,
            "paperless_url": paperless_url,
        })


if __name__ == "__main__":
    server = HTTPServer(("127.0.0.1", 8080), SetupHandler)
    print("Setup API listening on 127.0.0.1:8080")
    server.serve_forever()

```

Write to `one-click/setup/setup-api.py` and `chmod +x`.

- [ ] **Step 3: Commit**

```bash
chmod +x one-click/scripts/finalize-setup.sh one-click/setup/setup-api.py
git add one-click/scripts/finalize-setup.sh one-click/setup/setup-api.py
git commit -m "feat(one-click): add setup API and finalize script"

```

---

## Task 4: Setup Wizard Frontend

Create the static HTML page with the setup form, progress state, and
completion page.

**Files:**

- Create: `one-click/setup/wizard.html`

- [ ] **Step 1: Create wizard.html**

This is a single HTML file with inline CSS and JS. No external
dependencies. Three states: form, progress, complete.

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Paperless AI Setup</title>
<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body {
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
                 sans-serif;
    background: #f5f5f0;
    color: #333;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .card {
    background: #fff;
    border-radius: 12px;
    box-shadow: 0 2px 12px rgba(0,0,0,0.1);
    padding: 2.5rem;
    width: 100%;
    max-width: 480px;
    margin: 1rem;
  }
  h1 {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
    color: #2d5016;
  }
  .subtitle {
    color: #666;
    margin-bottom: 2rem;
    font-size: 0.95rem;
  }
  label {
    display: block;
    font-weight: 600;
    margin-bottom: 0.3rem;
    margin-top: 1rem;
    font-size: 0.9rem;
  }
  input, select {
    width: 100%;
    padding: 0.6rem 0.8rem;
    border: 1px solid #ccc;
    border-radius: 6px;
    font-size: 1rem;
  }
  input:focus, select:focus {
    outline: none;
    border-color: #2d5016;
    box-shadow: 0 0 0 2px rgba(45,80,22,0.15);
  }
  .hint {
    font-size: 0.8rem;
    color: #888;
    margin-top: 0.2rem;
  }
  .error {
    color: #c0392b;
    font-size: 0.85rem;
    margin-top: 0.3rem;
    display: none;
  }
  button {
    width: 100%;
    padding: 0.8rem;
    background: #2d5016;
    color: #fff;
    border: none;
    border-radius: 6px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    margin-top: 2rem;
  }
  button:hover { background: #3a6b1e; }
  button:disabled {
    background: #999;
    cursor: not-allowed;
  }
  .hidden { display: none; }

  /* Progress state */
  .spinner {
    width: 48px;
    height: 48px;
    border: 4px solid #e0e0e0;
    border-top-color: #2d5016;
    border-radius: 50%;
    animation: spin 0.8s linear infinite;
    margin: 2rem auto;
  }
  @keyframes spin { to { transform: rotate(360deg); } }
  .progress-text {
    text-align: center;
    color: #666;
    font-size: 1rem;
  }

  /* Completion state */
  .token-box {
    background: #f8f8f5;
    border: 1px solid #ddd;
    border-radius: 6px;
    padding: 0.8rem;
    font-family: monospace;
    font-size: 0.9rem;
    word-break: break-all;
    margin: 0.5rem 0;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
  .token-box code { flex: 1; }
  .copy-btn {
    width: auto;
    padding: 0.4rem 0.8rem;
    font-size: 0.8rem;
    margin-top: 0;
    white-space: nowrap;
  }
  .warning {
    background: #fff8e1;
    border: 1px solid #f0d060;
    border-radius: 6px;
    padding: 0.8rem;
    font-size: 0.85rem;
    margin-top: 1rem;
  }
  .ssh-hint {
    font-size: 0.85rem;
    color: #666;
    margin-top: 0.8rem;
  }
  .ssh-hint code {
    background: #f0f0f0;
    padding: 0.15rem 0.4rem;
    border-radius: 3px;
    font-size: 0.8rem;
  }
  .redirect-note {
    text-align: center;
    color: #888;
    font-size: 0.85rem;
    margin-top: 1.5rem;
  }
</style>
</head>
<body>

<!-- STATE 1: Form -->
<div class="card" id="form-card">
  <h1>Paperless AI Setup</h1>
  <p class="subtitle">Configure your document management system.</p>

  <form id="setup-form" novalidate>
    <label for="admin_user">Admin Username</label>
    <input type="text" id="admin_user" name="admin_user"
           autocomplete="username" required>

    <label for="admin_password">Admin Password</label>
    <input type="password" id="admin_password" name="admin_password"
           autocomplete="new-password" required>

    <label for="confirm_password">Confirm Password</label>
    <input type="password" id="confirm_password"
           autocomplete="new-password" required>
    <div class="error" id="pw-error">Passwords do not match.</div>

    <label for="timezone">Timezone</label>
    <select id="timezone" name="timezone" required>
      <option value="">Select timezone...</option>
      <option value="America/New_York">Eastern (America/New_York)</option>
      <option value="America/Chicago" selected>Central (America/Chicago)</option>
      <option value="America/Denver">Mountain (America/Denver)</option>
      <option value="America/Los_Angeles">Pacific (America/Los_Angeles)</option>
      <option value="America/Anchorage">Alaska (America/Anchorage)</option>
      <option value="Pacific/Honolulu">Hawaii (Pacific/Honolulu)</option>
    </select>

    <label for="domain">Domain Name <span style="font-weight:normal;color:#888">(optional)</span></label>
    <input type="text" id="domain" name="domain"
           placeholder="docs.yourfarm.com">
    <div class="hint">Leave blank to use your server's IP address.</div>

    <div id="email-group" class="hidden">
      <label for="le_email">Email for HTTPS Certificate</label>
      <input type="email" id="le_email" name="le_email"
             placeholder="you@example.com">
      <div class="hint">Used by Let's Encrypt. Not shared publicly.</div>
    </div>

    <div class="error" id="form-error"></div>

    <button type="submit" id="submit-btn">Set Up Paperless AI</button>
  </form>
</div>

<!-- STATE 2: Progress -->
<div class="card hidden" id="progress-card">
  <h1>Setting Up Paperless AI</h1>
  <div class="spinner"></div>
  <p class="progress-text">Starting services... this takes a few minutes.</p>
</div>

<!-- STATE 3: Complete -->
<div class="card hidden" id="complete-card">
  <h1>Paperless AI is Ready!</h1>
  <p class="subtitle">Your document management system is running.</p>

  <label>MCP Auth Token</label>
  <div class="token-box">
    <code id="mcp-token"></code>
    <button class="copy-btn" onclick="copyToken()">Copy</button>
  </div>
  <div class="warning">
    Save this token now. You need it to connect Claude to your
    Paperless instance.
  </div>
  <p class="ssh-hint">
    Lost your token later? SSH in and run:<br>
    <code>cat /opt/paperless-ai/.env | grep MCP_AUTH_TOKEN</code>
  </p>

  <a id="paperless-link" href="#">
    <button style="margin-top:1.5rem">Open Paperless</button>
  </a>
  <p class="redirect-note" id="redirect-note"></p>
</div>

<script>
(function() {
  var form = document.getElementById("setup-form");
  var domainInput = document.getElementById("domain");
  var emailGroup = document.getElementById("email-group");
  var pwError = document.getElementById("pw-error");
  var formError = document.getElementById("form-error");

  // Show/hide email field based on domain
  domainInput.addEventListener("input", function() {
    emailGroup.classList.toggle("hidden", !domainInput.value.trim());
  });

  form.addEventListener("submit", function(e) {
    e.preventDefault();
    pwError.style.display = "none";
    formError.style.display = "none";

    var pw = document.getElementById("admin_password").value;
    var confirm = document.getElementById("confirm_password").value;
    if (pw !== confirm) {
      pwError.style.display = "block";
      return;
    }
    if (pw.length < 8) {
      formError.textContent = "Password must be at least 8 characters.";
      formError.style.display = "block";
      return;
    }

    var payload = {
      admin_user: document.getElementById("admin_user").value.trim(),
      admin_password: pw,
      timezone: document.getElementById("timezone").value,
      domain: domainInput.value.trim(),
      le_email: document.getElementById("le_email").value.trim()
    };

    document.getElementById("submit-btn").disabled = true;

    fetch("/api/setup", {
      method: "POST",
      headers: {"Content-Type": "application/json"},
      body: JSON.stringify(payload)
    })
    .then(function(res) { return res.json().then(function(d) { return {ok: res.ok, data: d}; }); })
    .then(function(result) {
      if (!result.ok) {
        var msg = result.data.errors
          ? result.data.errors.join(", ")
          : result.data.error || "Setup failed";
        formError.textContent = msg;
        formError.style.display = "block";
        document.getElementById("submit-btn").disabled = false;
        return;
      }

      // Switch to progress state
      document.getElementById("form-card").classList.add("hidden");
      document.getElementById("progress-card").classList.remove("hidden");

      // Poll for completion
      var token = result.data.mcp_token;
      var url = result.data.paperless_url;
      pollCompletion(token, url);
    })
    .catch(function() {
      formError.textContent = "Connection error. Please try again.";
      formError.style.display = "block";
      document.getElementById("submit-btn").disabled = false;
    });
  });

  function pollCompletion(token, url) {
    var interval = setInterval(function() {
      fetch("/api/status")
        .then(function(res) { return res.json(); })
        .then(function(data) {
          if (data.state === "complete") {
            clearInterval(interval);
            showComplete(token, url);
          }
        })
        .catch(function() {
          // API may be down because host Caddy stopped --
          // that means finalize is running. Wait, then redirect.
          clearInterval(interval);
          setTimeout(function() { showComplete(token, url); }, 10000);
        });
    }, 3000);
  }

  function showComplete(token, url) {
    document.getElementById("progress-card").classList.add("hidden");
    document.getElementById("complete-card").classList.remove("hidden");
    document.getElementById("mcp-token").textContent = token;
    document.getElementById("paperless-link").href = url;

    // Countdown redirect
    var seconds = 30;
    var note = document.getElementById("redirect-note");
    var timer = setInterval(function() {
      seconds--;
      note.textContent = "Redirecting to Paperless in " + seconds + "s...";
      if (seconds <= 0) {
        clearInterval(timer);
        window.location.href = url;
      }
    }, 1000);
    note.textContent = "Redirecting to Paperless in 30s...";
  }
})();

function copyToken() {
  var token = document.getElementById("mcp-token").textContent;
  navigator.clipboard.writeText(token).then(function() {
    var btn = document.querySelector(".copy-btn");
    btn.textContent = "Copied!";
    setTimeout(function() { btn.textContent = "Copy"; }, 2000);
  });
}
</script>

</body>
</html>

```

Write to `one-click/setup/wizard.html`.

- [ ] **Step 2: Lint the HTML**

Run: `npx prettier --write one-click/setup/wizard.html`

- [ ] **Step 3: Commit**

```bash
git add one-click/setup/wizard.html
git commit -m "feat(one-click): add setup wizard HTML frontend"

```

---

## Task 5: Caddy Setup Config and Systemd Unit

Create the Caddy configuration for wizard mode and the systemd service
that runs both Caddy and the setup API on first boot.

**Files:**

- Create: `one-click/setup/Caddyfile.setup`
- Create: `one-click/systemd/paperless-setup.service`

- [ ] **Step 1: Create Caddyfile.setup**

```caddyfile
:80 {
    @api path /api/*
    handle @api {
        reverse_proxy localhost:8080
    }

    handle {
        file_server {
            root /opt/paperless-ai/setup
            index wizard.html
        }
    }
}

```

Write to `one-click/setup/Caddyfile.setup`.

- [ ] **Step 2: Create paperless-setup.service**

This systemd unit runs both the host Caddy (with the setup Caddyfile) and
the Python setup API. Uses `ExecStartPre` to start the API in the
background, then runs Caddy as the main process.

```ini
[Unit]
Description=Paperless AI Setup Wizard
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
ExecStartPre=/usr/bin/python3 /opt/paperless-ai/setup/setup-api.py &
ExecStart=/usr/bin/caddy run --config /opt/paperless-ai/setup/Caddyfile.setup
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target

```

Actually, `ExecStartPre` with `&` does not work in systemd. Use two
separate services instead. Replace the above with:

`one-click/systemd/paperless-setup-api.service`:

```ini
[Unit]
Description=Paperless AI Setup API
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
ExecStart=/usr/bin/python3 /opt/paperless-ai/setup/setup-api.py
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target

```

`one-click/systemd/paperless-setup.service`:

```ini
[Unit]
Description=Paperless AI Setup Wizard (Caddy)
After=network-online.target paperless-setup-api.service
Wants=network-online.target
Requires=paperless-setup-api.service

[Service]
Type=simple
ExecStart=/usr/bin/caddy run --config /opt/paperless-ai/setup/Caddyfile.setup
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target

```

Write both files to `one-click/systemd/`.

- [ ] **Step 3: Update finalize-setup.sh to stop both services**

In `one-click/scripts/finalize-setup.sh`, change the stop line:

Replace:

```bash
systemctl stop paperless-setup.service

```

With:

```bash
systemctl stop paperless-setup.service paperless-setup-api.service

```

And the disable line:

```bash
systemctl disable paperless-setup.service paperless-setup-api.service

```

- [ ] **Step 4: Commit**

```bash
git add one-click/setup/Caddyfile.setup one-click/systemd/
git add one-click/scripts/finalize-setup.sh
git commit -m "feat(one-click): add caddy setup config and systemd units"

```

---

## Task 6: First-Boot Script

Create the cloud-init script that runs when a user's droplet boots for
the first time.

**Files:**

- Create: `one-click/scripts/first-boot.sh`

- [ ] **Step 1: Create first-boot.sh**

```bash
#!/usr/bin/env bash
set -euo pipefail

# Regenerate SSH host keys (cleared during snapshot build)
dpkg-reconfigure openssh-server
systemctl restart ssh

# Start the setup wizard services
systemctl start paperless-setup-api.service
systemctl start paperless-setup.service

echo "Paperless AI setup wizard is ready on port 80"

```

Write to `one-click/scripts/first-boot.sh` and `chmod +x`.

- [ ] **Step 2: Commit**

```bash
git add one-click/scripts/first-boot.sh
git commit -m "feat(one-click): add cloud-init first-boot script"

```

---

## Task 7: Packer Template and Provision Script

Create the Packer HCL template and the shell provisioner that builds the
DigitalOcean snapshot.

**Files:**

- Create: `packer/paperless-ai.pkr.hcl`
- Create: `packer/provision.sh`
- Create: `packer/docker-compose.pull.yml`

- [ ] **Step 1: Create docker-compose.pull.yml**

A minimal compose file used only during the Packer build to pre-pull all
Docker images. No volumes, no healthchecks -- just the image references.

```yaml
services:
  db:
    image: pgvector/pgvector:pg16
  redis:
    image: redis:7-alpine
  paperless:
    image: ghcr.io/paperless-ngx/paperless-ngx:latest
  companion:
    image: ghcr.io/nerdoutinc/paperless-ai:latest
  caddy:
    image: caddy:2-alpine

```

Write to `packer/docker-compose.pull.yml`.

- [ ] **Step 2: Create provision.sh**

```bash
#!/usr/bin/env bash
set -euo pipefail

echo "=== Paperless AI Image Provisioning ==="

# 1. System updates
export DEBIAN_FRONTEND=noninteractive
apt-get update
apt-get upgrade -y

# 2. Install Docker CE + Docker Compose v2
apt-get install -y ca-certificates curl gnupg
install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg \
    | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
chmod a+r /etc/apt/keyrings/docker.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
    https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "$VERSION_CODENAME") stable" \
    > /etc/apt/sources.list.d/docker.list
apt-get update
apt-get install -y docker-ce docker-ce-cli containerd.io \
    docker-buildx-plugin docker-compose-plugin

# 3. Install Caddy (host-level, for setup wizard)
apt-get install -y debian-keyring debian-archive-keyring apt-transport-https
curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/gpg.key' \
    | gpg --dearmor -o /usr/share/keyrings/caddy-stable-archive-keyring.gpg
curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/debian.deb.txt' \
    > /etc/apt/sources.list.d/caddy-stable.list
apt-get update
apt-get install -y caddy

# Disable the default Caddy service (we use our own systemd unit)
systemctl disable caddy
systemctl stop caddy

# 4. Verify Python 3
python3 --version

# 5. Pre-pull all Docker images
mkdir -p /tmp/pull
cp /tmp/docker-compose.pull.yml /tmp/pull/docker-compose.yml
cd /tmp/pull
docker compose pull
cd /
rm -rf /tmp/pull

# 6. Copy application files into place
mkdir -p /opt/paperless-ai/{setup,templates,scripts,systemd,backups}
cp -r /tmp/one-click/setup/* /opt/paperless-ai/setup/
cp -r /tmp/one-click/templates/* /opt/paperless-ai/templates/
cp -r /tmp/one-click/scripts/* /opt/paperless-ai/scripts/
chmod +x /opt/paperless-ai/scripts/*.sh
chmod +x /opt/paperless-ai/setup/setup-api.py

# 7. Install systemd services
cp /tmp/one-click/systemd/paperless-setup.service /etc/systemd/system/
cp /tmp/one-click/systemd/paperless-setup-api.service /etc/systemd/system/
systemctl daemon-reload
systemctl enable paperless-setup.service
systemctl enable paperless-setup-api.service

# 8. Install cloud-init config
mkdir -p /var/lib/cloud/scripts/per-instance
cp /opt/paperless-ai/scripts/first-boot.sh \
    /var/lib/cloud/scripts/per-instance/01-paperless-ai.sh
chmod +x /var/lib/cloud/scripts/per-instance/01-paperless-ai.sh

# 9. Snapshot hygiene
apt-get clean
rm -rf /var/lib/apt/lists/*
truncate -s 0 /var/log/*.log /var/log/**/*.log 2>/dev/null || true
rm -f /etc/ssh/ssh_host_*
rm -f /root/.bash_history
cloud-init clean --logs

echo "=== Provisioning complete ==="

```

Write to `packer/provision.sh` and `chmod +x`.

- [ ] **Step 3: Create paperless-ai.pkr.hcl**

```hcl
packer {
  required_plugins {
    digitalocean = {
      version = ">= 1.1.0"
      source  = "github.com/digitalocean/digitalocean"
    }
  }
}

variable "do_api_token" {
  type      = string
  sensitive = true
  default   = env("DIGITALOCEAN_API_TOKEN")
}

source "digitalocean" "paperless-ai" {
  api_token    = var.do_api_token
  image        = "ubuntu-24-04-x64"
  region       = "nyc1"
  size         = "s-2vcpu-4gb"
  ssh_username = "root"
  snapshot_name = "paperless-ai-{{timestamp}}"
  snapshot_regions = [
    "nyc1", "nyc3", "sfo3", "ams3", "sgp1",
    "lon1", "fra1", "tor1", "blr1", "syd1"
  ]
}

build {
  sources = ["source.digitalocean.paperless-ai"]

  # Upload application files
  provisioner "file" {
    source      = "../one-click"
    destination = "/tmp/one-click"
  }

  provisioner "file" {
    source      = "docker-compose.pull.yml"
    destination = "/tmp/docker-compose.pull.yml"
  }

  # Run provisioning script
  provisioner "shell" {
    script = "provision.sh"
  }

  # Clean up uploaded files
  provisioner "shell" {
    inline = ["rm -rf /tmp/one-click /tmp/docker-compose.pull.yml"]
  }

  post-processor "manifest" {
    output     = "manifest.json"
    strip_path = true
  }
}

```

Write to `packer/paperless-ai.pkr.hcl`.

- [ ] **Step 4: Commit**

```bash
git add packer/
git commit -m "feat(one-click): add Packer template and provisioning script"

```

---

## Task 8: GitHub Actions Workflow

Create the CI workflow that builds the snapshot, updates
`deploy-config.json`, and cleans up old snapshots.

**Files:**

- Create: `.github/workflows/packer-build.yml`
- Create: `docs/deploy-config.json`

- [ ] **Step 1: Create deploy-config.json placeholder**

```json
{
  "snapshot_id": "",
  "built_at": "",
  "droplet_size": "s-2vcpu-4gb"
}

```

Write to `docs/deploy-config.json`. This will be auto-updated by CI.

- [ ] **Step 2: Create packer-build.yml**

```yaml
name: Build DigitalOcean Snapshot

on:
  schedule:
    - cron: "0 4 * * 0"  # Sundays at 4 AM UTC
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  DIGITALOCEAN_API_TOKEN: ${{ secrets.DIGITALOCEAN_API_TOKEN }}

jobs:
  build-snapshot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Packer
        uses: hashicorp/setup-packer@main
        with:
          version: latest

      - name: Init Packer plugins
        working-directory: packer
        run: packer init paperless-ai.pkr.hcl

      - name: Build snapshot
        working-directory: packer
        run: packer build paperless-ai.pkr.hcl

      - name: Extract snapshot ID
        id: snapshot
        working-directory: packer
        run: |
          SNAPSHOT_ID=$(jq -r '.builds[-1].artifact_id' manifest.json)
          echo "id=$SNAPSHOT_ID" >> "$GITHUB_OUTPUT"
          echo "Snapshot ID: $SNAPSHOT_ID"

      - name: Update deploy-config.json
        run: |
          cat > docs/deploy-config.json <<EOF
          {
            "snapshot_id": "${{ steps.snapshot.outputs.id }}",
            "built_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "droplet_size": "s-2vcpu-4gb"
          }
          EOF

      - name: Commit deploy-config.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/deploy-config.json
          git commit -m "ci: update snapshot ID to ${{ steps.snapshot.outputs.id }}"
          git push

      - name: Delete previous snapshot
        run: |
          # List all paperless-ai snapshots, delete all except the newest
          SNAPSHOTS=$(curl -s -X GET \
            -H "Authorization: Bearer $DIGITALOCEAN_API_TOKEN" \
            "https://api.digitalocean.com/v2/snapshots?resource_type=droplet&per_page=200" \
            | jq -r '.snapshots[] | select(.name | startswith("paperless-ai-")) | .id' \
            | sort -rn)

          KEEP="${{ steps.snapshot.outputs.id }}"
          for ID in $SNAPSHOTS; do
            if [ "$ID" != "$KEEP" ]; then
              echo "Deleting old snapshot: $ID"
              curl -s -X DELETE \
                -H "Authorization: Bearer $DIGITALOCEAN_API_TOKEN" \
                "https://api.digitalocean.com/v2/snapshots/$ID"
            fi
          done

```

Write to `.github/workflows/packer-build.yml`.

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/packer-build.yml docs/deploy-config.json
git commit -m "feat(one-click): add Packer CI workflow and deploy config"

```

---

## Task 9: Deploy Button on Landing Page

Add a "Deploy on DigitalOcean" button to the existing landing page that
reads `deploy-config.json` to construct the deploy link dynamically.

**Files:**

- Modify: `docs/index.html`

- [ ] **Step 1: Read the current landing page**

Read `docs/index.html` to understand the existing structure and find the
right place to add the deploy button.

- [ ] **Step 2: Add deploy button section**

Add a new section to the landing page with:

- A "Deploy on DigitalOcean" button styled to match the existing page
- JS that fetches `deploy-config.json` and constructs the link
- Brief instructions: what they'll get, cost, and the 3-step process
  (click, create droplet, visit IP)

The button should link to:
`https://cloud.digitalocean.com/droplets/new?image=SNAPSHOT_ID&size=s-2vcpu-4gb`

If `snapshot_id` is empty (not yet built), show the button as disabled
with "Coming Soon" text.

The exact HTML/CSS depends on the existing page structure -- read first,
then match the style.

- [ ] **Step 3: Lint the HTML**

Run: `npx prettier --write docs/index.html`

- [ ] **Step 4: Commit**

```bash
git add docs/index.html
git commit -m "feat(one-click): add Deploy on DigitalOcean button to landing page"

```

---

## Task 10: Documentation and Final Cleanup

Update project documentation to cover the 1-click deployment path.

**Files:**

- Modify: `docs/index.html` (if a docs/deploy page is needed)
- Create: `one-click/README.md`

- [ ] **Step 1: Create one-click/README.md**

Brief developer docs covering:

- What the `one-click/` directory contains
- How to build the snapshot locally with Packer
  (`cd packer && packer init . && packer build .`)

- Required env var: `DIGITALOCEAN_API_TOKEN`
- How the CI auto-builds weekly
- File layout on the resulting image (`/opt/paperless-ai/`)

Keep it concise -- 30-50 lines.

- [ ] **Step 2: Lint the markdown**

Run: `npx markdownlint-cli2 one-click/README.md`

- [ ] **Step 3: Commit**

```bash
git add one-click/README.md
git commit -m "docs: add one-click developer documentation"

```
